/**
 * ----------------------------------------------------------------------------
 * ADMIN CREDENTIALS
 * ----------------------------------------------------------------------------
 * Change the password on the line marked below, then rebuild and redeploy.
 *
 * These are compiled into the client bundle and are therefore readable by
 * anyone who inspects the JavaScript. They gate a panel that only edits data
 * held in this browser's localStorage — nothing here is a security boundary,
 * and no server-side data can be reached with them.
 */

export const ADMIN_USERNAME = 'admin'

/** ↓↓↓ CHANGE THE ADMIN PASSWORD ON THIS LINE ↓↓↓ */
export const ADMIN_PASSWORD = 'admin'
